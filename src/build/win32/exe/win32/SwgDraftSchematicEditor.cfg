# Read by SwgDraftSchematicEditor's own parser (Configuration::loadCfg,
# Configuration.cpp:312) via cms_root = "../../exe/win32/" -- NOT by the engine.
# The engine reads its own SwgDraftSchematicEditor.cfg from the working
# directory (x64/Release). Both files must exist; they are not interchangeable.
[SwgDraftSchematicEditor]
serverObjectTemplatePath=D:/SWG All Tools Working/swg/current/dsrc/sku.0/sys.server/compiled/game/object/
draftSchematicDirectory=draft_schematic
