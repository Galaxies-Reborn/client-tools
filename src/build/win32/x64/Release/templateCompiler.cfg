# templateCompiler.cfg (2026-08-29) - the compiler resolves @base template
# references through TreeFile; mount the SOE reference data root so
# object/... names resolve. Legacy no-sku keys (tool passes no skuBits).
# QUOTED - the path has spaces.
[SharedFile]
	searchPath0="D:/SWG All Tools Working/swg/current/data/sku.0/sys.shared/compiled/game"
[SharedFoundation]
	demoMode=1
